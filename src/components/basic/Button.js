import React from 'react';
import { Text as RNText } from 'react-native';

const Button = props => {
  return <RNText {...props} />;
};

Button.propTypes = {};

Button.defaultProps = {};

export default Button;
