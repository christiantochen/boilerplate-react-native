import React from 'react';
import { Text as RNText } from 'react-native';

const Text = props => {
  return <RNText {...props} />;
};

Text.propTypes = {};

Text.defaultProps = {};

export default Text;
