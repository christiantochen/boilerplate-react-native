/*
 * Reducer actions related with login
 */
import * as types from '../../fixtures/actionTypes';

export function onSplashScreenReady() {
    return {
        type: types.SPLASH_SCREEN_READY
    }
}
