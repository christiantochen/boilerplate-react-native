import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    icon: {
        alignSelf: 'center',
        color: '#FFFFFF',
        marginHorizontal: 24
    }
});

export default styles;